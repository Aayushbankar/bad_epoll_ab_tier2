# Verification Ledger

This document serves as the authoritative, machine-parseable Single Source of Truth (SSOT) mapping every verified research claim to its physical evidence, target kernel symbol, execution method, and raw log artifact.

---

## Verified Claims & Physical Evidence Matrix

| Verification ID | Date & Timestamp (UTC) | Claim / Fact Description | Target Symbol / Address | Verification Method | Raw Evidence File | Status |
|---|---|---|---|---|---|---|
| VER-009 | 2026-07-23 | Runtime observation of single-watch pipe_buffer cross-cache UAF on struct file->f_ep | __ep_remove | RUNTIME | tier2/evidence/EXP-006_raw_gdb.log | INCONCLUSIVE |
| VER-010 | 2026-07-24 | ~~Runtime observation of single-watch NULL stale-write triggering kernel panic in __mutex_lock_slowpath~~ | __list_add_valid | RUNTIME | INVALID (Protocol Violation) | **RETRACTED** — see VER-012 |
| VER-011 | 2026-07-24 | `struct epitem` is 120 bytes, allocated from the dedicated `eventpoll_epi` slab cache (NOT `kmalloc-192`). The cache is created with `SLAB_HWCACHE_ALIGN|SLAB_PANIC|SLAB_ACCOUNT` at `eventpoll.c:2555`. The stale write is `list_del_init(&epi->rdllink)` targeting offsets 24 and 32, not a NULL write to offset 160. | `struct epitem` | STATIC (GDB `ptype /o`, kernel source) | tier2/evidence/2026-07-24/ep_remove_disassembly.txt | **VERIFIED** |
| VER-012 | 2026-07-24 | `__ep_remove` is called via `ep_eventpoll_release` → `ep_clear_and_put` → `ep_remove_safe` when closing `outer_epoll`. GDB breakpoint confirms entry with `ep=0xffffff807fa1e000`, `epi=0xffffff800438ef00`. Stale-write instructions are at `__ep_remove` offsets +284, +288, +292, +296. | `__ep_remove` @ `0xffffffc0083bce2c` | RUNTIME (GDB trace) | tier2/evidence/2026-07-24/gdb_uaf_trace_first_pass.log | **VERIFIED** |
| VER-013 | 2026-07-24 | `__mutex_lock_slowpath` fires during `snd_timer_user_ioctl` contention, but `wait_list.next` is self-pointing (normal, NOT corrupted). The `snd_timer_user` mutex is NOT the UAF target because `snd_timer_user` (~168 bytes) is in `kmalloc-192`, not the dedicated `eventpoll_epi` cache where the freed epitem lives. | `__mutex_lock_slowpath` @ `0xffffffc008f60948` | RUNTIME (GDB trace) | tier2/evidence/2026-07-24/gdb_uaf_trace_first_pass.log | **VERIFIED** (normal behavior, not corruption) |

## Retraction Log

| VER ID | Date Retracted | Reason |
|--------|---------------|--------|
| VER-010 | 2026-07-24 | Claim was "NULL stale-write at offset 160 consumed by mutex slowpath." Retracted because: (1) the stale write is `list_del_init` at offsets 24/32, not NULL at 160; (2) the UAF victim is in dedicated `eventpoll_epi` cache, not `kmalloc-192`; (3) the mutex slowpath hit was normal contention, not corruption. Original task log `INVALID (Protocol Violation)` may not survive session boundaries. |
| VER-029 | 2026-08-02 | Dual-watch KASLR leak claim retracted. EXP-024 proved the race conditions are mutually exclusive: UAF requires single-epitem (f_ep=NULL), kernel-pointer write requires multi-epitem. These cannot co-occur. Original EXP-022b log had inverted before/after labels and no msgrcv readback. See `tier2/evidence/EXP-024_RESULTS.md`. |
| VER-030 | 2026-08-02 | KASLR defeat claim retracted. Dependent on VER-029 which was disproved. No kernel pointer leak was ever demonstrated reaching userspace. |
| VER-044 (transient cf24bd6 CONFIRMED claim) | 2026-09-06 | Retracted claim that Hypothesis 2 is CONFIRMED on GKI 6.1.23 based on a single hit in commit cf24bd6. While the detection mechanism was sound (ep_alloc uses kzalloc, so memory is zeroed; not a false positive), the race could not be reproduced across 15,000 additional iterations (1/20,000 total; ~0.005%). The race can theoretically fire under emulation but is extremely rare. Status updated to INCONCLUSIVE. See tier2/evidence/HYP-002/HYP-002_RESULTS_GKI.md. |
| VER-063 | 2026-09-07 (retracted 2026-09-13 EXP-AUDIT) | Claim "swaps_poll successfully wrote 0 to specified target addresses" — raw log lines 324-326 show 3x "GATED CHECK FAILED". No swaps_poll fire ever occurred. See EVO-030. |
| VER-064 | 2026-09-07 (retracted 2026-09-13 EXP-AUDIT) | Claim "Root Privilege Escalation achieved (getuid()==0)" — harness ran as uid=0 throughout (privilege drop was added post-hoc). getuid()==0 was vacuous. No write ever fired (depends on VER-063). See EVO-031. |

## Evolution Notes

| EVO ID | Date | Note |
|--------|------|------|
| EVO-005 | 2026-07-24 | Corrected slab cache from `kmalloc-192` to dedicated `eventpoll_epi` cache (120 bytes, `SLAB_HWCACHE_ALIGN|SLAB_ACCOUNT`). All prior "offset 160" analysis is invalid. The `snd_timer_user` open cannot reclaim the freed epitem because they are in different slab caches. Same-cache reclaim via `epoll_ctl(EPOLL_CTL_ADD)` is the viable strategy. |
| EVO-006 | 2026-07-27 | Confirmed `eventpoll_epi` is a truly isolated dedicated cache. `CONFIG_MEMCG` is disabled (`.config` line: `# CONFIG_MEMCG is not set`), so `kmalloc-cg-128` does not exist. The cache's `SLAB_ACCOUNT` flag prevents merging into `kmalloc-128` (which lacks `SLAB_ACCOUNT`). Cross-cache reclaim from generic kmalloc caches is NOT possible. Only same-cache reclaim (allocating new `epitem` objects via `epoll_ctl`) can reclaim the freed slot. Source: `eventpoll.c:2555` (`kmem_cache_create`), `slab_common.c:173-215` (`find_mergeable`). |
| VER-014 | 2026-07-27 | Verified `eventpoll_epi` slab cache (used for `struct epitem`) is created with `SLAB_ACCOUNT` (eventpoll.c:2555). In `mm/slab_common.c:51/200`, `SLAB_ACCOUNT` is part of `SLAB_MERGE_SAME`, preventing merge with standard `kmalloc-128` (which lacks this flag). Cache is completely isolated. | `eventpoll_epi` | STATIC (Source code audit) | third_party/linux-6.12.67/fs/eventpoll.c, third_party/linux-6.12.67/mm/slab_common.c | **VERIFIED** |
| VER-015 | 2026-07-27 | Same-cache reclaim via `epoll_ctl(EPOLL_CTL_ADD)` successfully occupies the freed `epitem` slot. Freed `epitem` address (e.g., `0xffffff80043cb400`) perfectly matches the address of a newly allocated `epitem` during spray. | `struct epitem` | RUNTIME (GDB trace) | tier2/evidence/EXP-007_raw_gdb.log | **VERIFIED** |
| VER-016 | 2026-07-28 | EXP-008 Timing Analysis: The `list_del_init` corrupting write on `epi->rdllink` executes *before* `call_rcu(epi)` in `__ep_remove`. Therefore, the memory is corrupted before it is freed. When reclaimed via `epoll_ctl(EPOLL_CTL_ADD)`, the newly allocated `struct epitem` has its `rdllink` unavoidably overwritten by `INIT_LIST_HEAD` during initialization (`str x0, [x24, #0x18]; str x0, [x24, #0x20]`), destroying the `LIST_POISON` values. This makes the same-cache `list_del_init` path a structural dead-end regardless of allocation flags. | `__ep_remove` | RUNTIME (GDB trace) | tier2/evidence/EXP-008_raw_gdb.log | **VERIFIED** |
| VER-017 | 2026-07-29 | Reclaimed exact file pointer `0xffffff800233de00` via `open()` spraying (EXP-009). | `struct file` | RUNTIME (GDB trace) | tier2/evidence/EXP-009_final.log | **VERIFIED** |
| VER-018 | 2026-07-29 | EXP-010: `filp` cache (for `struct file`) is created with `SLAB_TYPESAFE_BY_RCU`, making it unmergeable (`slab_common.c:500`). `pipe_buffer` is allocated via `kcalloc` (generic `kmalloc-cg`). Additionally, `pipe_buffer` allocation size (40 bytes * power of 2) mathematically misses the 256-byte size class. Cross-cache reclaim from `pipe_buffer` to `struct file` is a structural dead end. | `filp_cachep` / `pipe_buffer` | STATIC (Source code audit) | tier2/evidence/EXP-010_RESULTS.md | **VERIFIED** |
| VER-019 | 2026-07-31 | EXP-011: Type-confusion on the reclaimed `struct file` via `ep_item_poll` is fundamentally unreachable. Any thread triggering the UAF race in `__ep_remove` must hold `ep->mtx`. Concurrently, any attempt to interact with the stale `epitem` (via `epoll_wait` or `EPOLL_CTL_MOD`) also blocks on `ep->mtx`. When the racing thread drops `ep->mtx`, the `epitem` has already been fully unlinked and scheduled for RCU destruction. Thus, userspace cannot access the stale `epitem` to poll the reclaimed file. (Note: Downgraded to INCONCLUSIVE on 2026-07-30 due to missing evidence in raw log; RE-VERIFIED 2026-07-31 via complete hardware-breakpoint trace). | `__ep_remove` | RUNTIME (HW Breakpoint Trace) | tier2/evidence/EXP-011_raw_gdb.log | **VERIFIED** |
| EVO-007 | 2026-07-30 | **THEORY ONLY**: Proposes UAF victim is `struct eventpoll` in `kmalloc-192`, with a `NULL` stale write at offset 160 via `hlist_del_rcu(&epi->fllink)`. This matches the retracted VER-006/VER-007 theory. Conclusively disproved by VER-021. |
| VER-020 | 2026-07-30 | Verified statically via GDB that `sizeof(struct eventpoll)` is 176 bytes (placing it in `kmalloc-192`) and the offset of the `refs` field (`struct hlist_head`) is 160 (`0xa0`). | `struct eventpoll` | STATIC (GDB `sizeof`, `print &...`) | tier2/evidence/EVO-007_CORRECTION.md | **VERIFIED** |
| VER-021 | 2026-07-31 | EXP-013: Live-trace hardware watchpoint confirms `hlist_del_rcu` writes to `inner_epoll_struct+160` BEFORE `ep_eventpoll_release` frees the struct (Single thread scenario). | `__ep_remove` / `ep_eventpoll_release` | RUNTIME (GDB trace) | tier2/evidence/EXP-013_RESULTS.md | **INCONCLUSIVE** (Disproved for multi-thread scenario by VER-026) |
| VER-022 | 2026-07-31 | `remove_wait_queue` fully drains `epi` BEFORE it is corrupted in `__ep_remove`. | `ep_clear_and_put` | RUNTIME (Identity verified, Concurrency caveat noted) | `tier2/evidence/EXP-012_RESULTS.md` | **VERIFIED** |
| VER-023 | 2026-07-31 | `__ep_remove` memory modification audit | `__ep_remove` | STATIC (Source + Disassembly Audit) | `tier2/docs/EP_REMOVE_AUDIT.md` | **VERIFIED** |
| VER-024 | 2026-07-31 | Thread B `eventpoll_release_file` dereferencing stale `epi` (Wait: Conflates locking logic, under review). | `eventpoll_release_file` | STATIC (Source Analysis) | `tier2/evidence/EXP-014_RESULTS.md` | **DISPROVED** (Replaced by VER-026 lockless path) |
| VER-025 | 2026-07-31 | EXP-015: `struct file` UAF (`filp` cache) is a structural dead end. The `filp` cache exclusively allocates `struct file`, so any reclaimed object will have valid `f_op` and `f_lock`. Thread A's stale accesses (`is_file_epoll` reading `f_op`, and `spin_unlock` writing `0` to `f_lock`) safely evaluate to `false` and perform a harmless no-op respectively. | `struct file` / `filp` | STATIC (Source Code & Disassembly Audit) | `tier2/evidence/EXP-015_RESULTS.md` | **VERIFIED** |
| EVO-008 | 2026-07-31 | **THEORY ONLY**: The `kmalloc-192` UAF on `struct eventpoll` triggers via a multi-threaded race. If Thread A (`__ep_remove`) clears `f_ep` and is preempted, Thread B (`__fput`) will see `f_ep == NULL` and take the lockless path bypassing `eventpoll_release_file` entirely. Thread B frees the `struct eventpoll` before Thread A wakes up to execute `hlist_del_rcu`, writing `NULL` to offset 160. | `struct eventpoll` | STATIC (Multi-Threaded Logic) | `tier2/evidence/EXP-015_RESULTS.md` | **VERIFIED** (See VER-026) |
| VER-026 | 2026-07-31 | EXP-015 HW TRACE: Two-threaded race definitively captured. Thread A (`close(outer)`) sets `f_ep = NULL` and suspends. Thread B (`close(inner)`) takes lockless fast-path in `eventpoll_release`, bypasses `eventpoll_release_file`, and fully frees `inner_epoll` to `kmalloc-192`. Thread A resumes and `hlist_del_rcu` writes `NULL` to offset `160` (`refs.first`) of the FREED `struct eventpoll`. | `eventpoll_release` / `__ep_remove` | RUNTIME (HW Watchpoint Trace) | `tier2/evidence/EXP-015_unified_trace.log` | **VERIFIED** |
| VER-027 | 2026-08-01 | EXP-018: `msg_msg` spray with 144-byte user data reliably reclaims freed `struct eventpoll` in `kmalloc-192`. Attacker-controlled content lands at all offsets from 48 onward. Verified via before/after memory dumps showing the `0x41414141` spray payload at offset 48 (`0xffff0000036bdb70`). | `struct eventpoll` / `kmalloc-192` | RUNTIME (GDB trace) | `tier2/evidence/EXP-018_raw_gdb.log` | **VERIFIED** |
| VER-043 | 2026-08-01 | EXP-019: Building on VER-027's reclaim primitive, crafted `msg_msg` payload successfully placed an invalid pointer marker (`0xdead000000000000`) precisely at offset 136 (`ep->user`). Verified via before/after memory dumps at `0xffff0000035c4a48`. However, Thread A did not crash (see VER-028). | `struct eventpoll` / `kmalloc-192` | RUNTIME (GDB trace) | `tier2/evidence/EXP-019_raw_gdb.log` | **VERIFIED** |
| VER-044 | 2026-09-06 | HYP-002: Kernel-side debugfs counters confirm natural race is extremely rare on Android GKI 6.1.23 under QEMU TCG: 1 hit in initial 5,000 run (cf24bd6), but 0 hits across 35,000 reproduction iterations (3x 5,000 runs + 20,000 continuous batch). Detection mechanism is sound (kzalloc guarantees zeroed memory on allocation; debug_freed=0 is redundant but harmless). The race CAN fire on 6.1.23 under TCG, but at an extremely low frequency (~0.0025%, 95% CI upper bound <0.00857%). | `__ep_remove` / `ep_free` / `hlist_del_rcu` | RUNTIME (Kernel Custom Counters) | `tier2/evidence/HYP-002/HYP-002_repro_run1.log`, `tier2/evidence/HYP-002/HYP-002_repro_run2.log`, `tier2/evidence/HYP-002/HYP-002_repro_run3.log`, `tier2/evidence/HYP-002/HYP-002_repro_batch20k.log`, `tier2/evidence/HYP-002/HYP-002_RESULTS_GKI.md` | **INCONCLUSIVE** (Rare Hit: 1/40,000) |
| VER-028 | 2026-08-01 | EXP-019: `percpu_counter_dec(&ep->user->epoll_watches)` in `__ep_remove` does NOT dereference the FREED eventpoll's `user` pointer. In the outer-close/inner-close race, `ep` parameter is the OUTER eventpoll (valid), not the freed INNER eventpoll. Only `hlist_del_rcu(&epi->fllink)` at offset 160 is a UAF write on the freed slot. Chain 0 controlled-crash hypothesis disproved. | `__ep_remove` | RUNTIME (GDB trace) | `tier2/evidence/EXP-019_raw_gdb.log` | **VERIFIED** (Negative Result) |
| VER-029 | 2026-08-01 | ~~EXP-022b: Dual-watch topology leaks kernel heap pointer via hlist_del_rcu writing epi2 address to freed inner_epoll->refs.first (offset 160).~~ | `hlist_del_rcu` / `inner_epoll->refs` | RUNTIME (GDB trace) | `tier2/evidence/EXP-022b_raw_gdb.log` | **RETRACTED** — see VER-033 |
| VER-030 | 2026-08-01 | ~~EXP-022b: Leaked epitem address defeats KASLR.~~ | KASLR / kernel heap | RUNTIME + STATIC | `tier2/evidence/EXP-022b_raw_gdb.log` | **RETRACTED** — see VER-033 |
| VER-033 | 2026-08-02 | EXP-024: Dual-watch KASLR leak claim DISPROVED. With 2+ epitems in inner_epoll->refs, the single-epitem condition (eventpoll.c:826) is FALSE, so WRITE_ONCE(f_ep, NULL) does NOT execute. Without f_ep=NULL, the lockless-bypass race (VER-026) cannot trigger, and inner_epoll is never freed before hlist_del_rcu. Hardware watchpoint confirmed: write went to LIVE memory (ep_free_inner_seen=False), value was NULL. The conditions for UAF (single-epitem) and kernel-pointer write (multi-epitem) are mutually exclusive. | `hlist_del_rcu` / `inner_epoll->refs` | RUNTIME (GDB HW watchpoint) | `tier2/evidence/EXP-024_raw_gdb.log`, `tier2/evidence/EXP-024_RESULTS.md` | **VERIFIED** (Negative Result) |
| VER-031 | 2026-08-01 | EXP-023b: `percpu_counter_dec` in `__ep_remove` operates on the OUTER eventpoll's user_struct (valid), not the freed INNER eventpoll. GDB-assisted overwrite of outer_epoll->user failed to redirect the decrement. Breakpoint at percpu_counter_add_batch confirms fbc = root_user+8. Chain 2 arbitrary decrement not achievable in outer-close/inner-close race. | `percpu_counter_dec` / `__ep_remove` | RUNTIME (GDB trace) | `tier2/evidence/EXP-023b_raw_gdb.log` | **VERIFIED** (Negative Result) |
| VER-032 | 2026-08-01 | EXP-023b: The ONLY verified UAF write on the freed eventpoll in this race is `hlist_del_rcu(&epi->fllink)` at offset 160 (NULL with 1 epitem, kernel pointer with 2+ epitems). All other operations (rb_erase_cached, spin_lock_irq, percpu_counter_dec) operate on the OUTER epoll. Future chains must target hlist_del_rcu primitive. | `hlist_del_rcu` / freed eventpoll | RUNTIME (GDB trace) | `tier2/evidence/EXP-023b_raw_gdb.log` | **VERIFIED** |

---

## Phase 3: Natural Schedulability & Android Portability (PLANNED — Not Yet Verified)

| Verification ID | Date & Timestamp (UTC) | Claim / Fact Description | Target Symbol / Address | Verification Method | Raw Evidence File | Status |
|---|---|---|---|---|---|---|
| VER-034 | 2026-08-02 | NAT-001: Race naturally schedulable without GDB — hit rate in 10,000 iterations | `__ep_remove` / `hlist_del_rcu` | RUNTIME (Statistical) | `tier2/evidence/NAT-001/NAT-001_raw_serial.log` | **FAILED** (0/10,000 hits; CI upper bound 0.0384%) |
| VER-035 | 2026-08-02 | NAT-002: cond_resched at lines 888/903 exists but is NO-OP in 2-CPU pinned PREEMPT_VOLUNTARY | `ep_clear_and_put` / `ep_remove_safe` / `__ep_remove` | STATIC-HIGH-CONFIDENCE | `tier2/evidence/NAT-002/NAT-002_RESULTS.md` | **CORRECTED** |
| VER-036 | 2026-08-02 | NAT-003: msg_msg reclaim works under natural timing (no GDB window) | `load_msg` / `kmalloc-192` | RUNTIME (Statistical reclaim stats) | `tier2/evidence/NAT-003_raw_*.log` | **PLANNED** |
| VER-037 | 2026-08-02 | NAT-004: Per-CPU partial slab interferes with cross-CPU reclaim | `kmalloc-192` / `SLUB_CPU_PARTIAL` | RUNTIME (Same-CPU vs cross-CPU) | `tier2/evidence/NAT-004_raw_*.log` | **PLANNED** |
| VER-038 | 2026-08-05 | AND-001: SysV IPC (`msgsnd`/`msgrcv`) works in target kernel context; trapped runtime call at `load_msg` (`0xffff800080430100`) | `load_msg` / `msgget` / `msgsnd` | RUNTIME (GDB Breakpoint) | `tier2/evidence/AND-001_raw_ipc.log` | **VERIFIED** |
| VER-039 | 2026-08-05 | NAT-005: 92,740 iteration upgraded closed-loop search (isolcpus=1 CPU isolation + 4MB memory cache eviction sweeper, 10-cycle steps) produced 0/92,740 UAF hits; near-miss alignment error reached 1 cycle (16 ns) at delay=2360 under TCG software emulation | `__ep_remove` / `hlist_del_rcu` | RUNTIME (Upgraded Closed-Loop Sweep) | `tier2/evidence/NAT-005_raw_serial.log` | **VERIFIED** (Negative Result under TCG) |
| VER-042 | 2026-08-09 | AND-002: KASLR does not alter the underlying race mechanics (alignment error ~0-3 cycles) and hit rate remains 0 whether KASLR is enabled or disabled. | `__ep_remove` / `kmalloc-192` | RUNTIME (KASLR on vs off) | `tier2/evidence/AND-002_raw_kaslr_on.log` | **VERIFIED** |
| VER-040 | 2026-08-08 | AND-003: SELinux enforcing allows exploit syscalls (`epoll`, `msgget`, `msgsnd`, `msgrcv`) | `epoll_create1` / `msgget` / etc. | RUNTIME (SELinux audit) | `tier2/evidence/AND-003_raw_enforcing.log` | **VERIFIED** |
| VER-041 | 2026-08-02 | AND-004: MTE/KASAN_HW_TAGS does not prevent UAF detection | `hlist_del_rcu` / MTE tags | RUNTIME (kasan=on + MTE) | `tier2/evidence/AND-004_raw_*.log` | **PLANNED** |
| VER-043 | 2026-09-05 | HYP-002: Kernel-side debugfs counters confirm natural race produces exactly 0 hits under QEMU TCG over 5000 iterations. Userspace oracle is completely accurate; race is genuinely failing. | `__ep_remove` / `ep_free` / `hlist_del_rcu` | RUNTIME (Kernel Custom Counters) | `tier2/evidence/HYP-002/HYP-002_raw_serial.log` | **VERIFIED** |
| VER-044 | 2026-09-06 | HYP-002: Kernel-side debugfs counters confirm natural race is extremely rare on Android GKI 6.1.23 under QEMU TCG: 1 hit in initial 5,000 run (cf24bd6), but 0 hits across 35,000 reproduction iterations (3x 5,000 runs + 20,000 continuous batch). Detection mechanism is sound (kzalloc guarantees zeroed memory on allocation; debug_freed=0 is redundant but harmless). The race CAN fire on 6.1.23 under TCG, but at an extremely low frequency (~0.0025%, 95% CI upper bound <0.00857%). | `__ep_remove` / `ep_free` / `hlist_del_rcu` | RUNTIME (Kernel Custom Counters) | `tier2/evidence/HYP-002/HYP-002_repro_run1.log`, `tier2/evidence/HYP-002/HYP-002_repro_run2.log`, `tier2/evidence/HYP-002/HYP-002_repro_run3.log`, `tier2/evidence/HYP-002/HYP-002_repro_batch20k.log`, `tier2/evidence/HYP-002/HYP-002_RESULTS_GKI.md` | **INCONCLUSIVE** (Rare Hit: 1/40,000) |
| VER-045 | 2026-09-06 | HYP-001: Timerfd delivery under QEMU TCG has a hard floor of 85.58us and mean 256.69us for 1us target (256.7x expansion). A 100-timerfd storm at 1us coalesces 88,164 ticks/wakeup and delivers only 203 hardware timer IRQs across 10,000 trials. Critical section widening percentage is unaffected (7.35% baseline vs 5.59% storm). Proves non-reproduction in NAT-001 is a QEMU TCG virtualization artifact, not bare-metal kernel behavior. | `timerfd` / `arch_timer` | RUNTIME (Statistical latency/jitter measurement) | `tier2/evidence/HYP-001/HYP-001_raw_serial.log`, `tier2/evidence/HYP-001/HYP-001_RESULTS.md` | **VERIFIED** |
| VER-046 | 2026-09-06 | EXP-027: Mass-freeing 32,000 struct file objects via 40 forked processes drained 1,991 out of 2,000 filp slab pages (99.55% efficiency) back to the buddy allocator within 1 second of RCU grace period expiration (slabs: 2 -> 2002 -> 11). Audit of GKI 6.1.23 confirmed CONFIG_DMABUF_HEAPS=y but # CONFIG_DMABUF_HEAPS_SYSTEM is not set, meaning /dev/dma_heap/system is absent on this kernel build. | `struct file` / `filp` slab / `/dev/dma_heap` | RUNTIME (SLUB sysfs counters) + STATIC (.config) | `tier2/evidence/EXP-027/EXP-027_raw_serial.log`, `tier2/evidence/EXP-027/EXP-027_RESULTS.md` | **VERIFIED** |
| VER-047 | 2026-09-06 | EXP-016b: DWARF audit of all 431 structs in kmalloc-192 (128 < size <= 192 bytes) for repeated-reset gadgets at offset 160 surfaced binder_device->ref (refcount_t at off 160; protected by refcount underflow/saturation traps), fib4_rule->srcmask (requires CAP_NET_ADMIN), and x509_certificate->blacklisted (transient object). No candidate provides an unprivileged, viable LPE repeated-reset primitive in kmalloc-192 under Android GKI 6.1.23. | `kmalloc-192` / offset 160 | STATIC (Single-pass pahole audit) | `tier2/evidence/EXP-016b/audit_gki_candidates.json`, `tier2/evidence/EXP-016b/EXP-016b_RESULTS.md` | **VERIFIED** (Negative Result) |
| VER-048 | 2026-09-06 | HYP-003: Disassembly of __ep_remove in GKI 6.1.23 vmlinux confirmed epi->ffd.file is loaded at +48 (ldr x22, [x1, #48]) without epi_fget() pinning. Source audit confirmed epi_fget() is completely absent from GKI 6.1.23 fs/eventpoll.c. Kernel is confirmed vulnerable to dangling struct file exploitation path. | `__ep_remove` / `epi->ffd.file` | STATIC (Binary Disassembly + Source Audit) | `tier2/evidence/HYP-003/disasm_ep_remove_raw.txt`, `tier2/evidence/HYP-003/HYP-003_RESULTS.md` | **VERIFIED** (Vulnerable) |
| VER-049 | 2026-09-06 | HYP-004: Evaluated order-0 buddy page reclaim alternatives when DMABUF_HEAPS_SYSTEM is absent. Runtime test confirmed memfd_create allocates order-0 buddy pages that are live-editable via MAP_SHARED from unprivileged userspace. pipe_buffer allocates order-0 buddy pages but rejects mmap. Rebuilding GKI with CONFIG_DMABUF_HEAPS_SYSTEM=y compiles cleanly (system_heap.o). | `memfd_create` / `pipe_buffer` / `system_heap` | RUNTIME (QEMU execution) + STATIC (Kconfig/Makefile) | `tier2/evidence/HYP-004/HYP-004_raw_serial.log`, `tier2/evidence/HYP-004/HYP-004_RESULTS.md` | **VERIFIED** |
| VER-050 | 2026-09-06 | HYP-005: Verified ep_show_fdinfo in fs/eventpoll.c:941 reads file_inode(epi->ffd.file)->i_ino. Runtime test in QEMU confirmed reading /proc/self/fdinfo/<epfd> from unprivileged UID 1000 outputs watched inode number (ino:204a), confirming an arbitrary address read (AAR) primitive when the watched struct file is controlled. | `ep_show_fdinfo` / `/proc/self/fdinfo` | RUNTIME (QEMU execution) + STATIC (Source code) | `tier2/evidence/HYP-005/HYP-005_raw_serial.log`, `tier2/evidence/HYP-005/HYP-005_RESULTS.md` | **VERIFIED** |
| VER-051 | 2026-09-06 | HYP-006: Verified swaps_poll in mm/swapfile.c:2564. Disassembly in vmlinux confirmed it writes atomic_read(&proc_poll_event) to private_data + 96 (str w1, [x19, #96]). Prototype matches file_operations.poll, satisfying Clang kCFI without violation. Runtime QEMU execution of poll() on /proc/swaps verified. | `swaps_poll` / `mm/swapfile.c` | RUNTIME (QEMU execution) + STATIC (Binary Disassembly) | `tier2/evidence/HYP-006/HYP-006_raw_serial.log`, `tier2/evidence/HYP-006/HYP-006_RESULTS.md` | **VERIFIED** |
| VER-052 | 2026-09-07 | HYP-007: Struct file unpinned reference race verified on Android GKI 6.1.23 under QEMU TCG. Custom debugfs instrumentation in __ep_remove and file_free detected unpinned access with file_count <= 0 in 4,941 out of 5,000 iterations (98.82%). Full slab-freed struct file UAF (file->f_version == 0xDEADF11E before spin_lock) was 0/5,000 due to QEMU TCG execution scheduling completing __ep_remove prior to Thread B's task_work ____fput completion. | `__ep_remove` / `epi->ffd.file` / `spin_lock(&file->f_lock)` | RUNTIME (Kernel Custom Counters in QEMU TCG) | `tier2/evidence/HYP-007/HYP-007_raw_serial.log`, `tier2/evidence/HYP-007/HYP-007_RESULTS.md` | **VERIFIED** |
| VER-053 | 2026-09-07 | HYP-008: Forced survivor-epoll state verified via GDB state injection (Variant B) under QEMU TCG (nokaslr, backported GKI 6.1.23). Bypassing `eventpoll_release_file` during Thread B's `close()` allowed Thread B to complete `task_work` `file_free()` while `epA` retained `epiA` in `epA->rbr` holding a dangling pointer to freed `inner_file` (`0xffffff800306be00`). In-tree source audit verified Hypothesis H-a (`fs/eventpoll.c:788`), confirming scheduling alone (Variant A) cannot clear `f_ep` while `epA` retains its watch. Proves downstream mechanics of survivor state. | `__fput` / `eventpoll_release` / `epA->rbr` | RUNTIME (GDB-forced state injection) + STATIC (Source derivation) | `tier2/evidence/HYP-008/HYP-008_raw_gdb.log`, `tier2/evidence/HYP-008/HYP-008_RESULTS.md` | **VERIFIED (GDB-Forced)** |
| VER-054 | 2026-09-07 | HYP-008: Arbitrary Address Read (AAR) oracle calibrated via `/proc/self/fdinfo/<epA_fd>` on Android GKI 6.1.23. Read path executed through `ep_show_fdinfo` on the unpinned/freed struct file without kernel panic or crash across both baseline and post-drain states. Evaluated filp slab draining (32,000 files) and buddy page spray (2,048 memfd pages). Result classified as PARTIAL (oracle safety confirmed; reclaim miss on partial slab did not cause crash, confirming oracle contract for retrying in natural exploit loop). Target `init_task.comm` address calibrated at `0xffffffc00a0cc718` (`0x0072657070617773` / "swapper"). | `ep_show_fdinfo` / `/proc/self/fdinfo` / `init_task.comm` | RUNTIME (QEMU execution + GDB trace) | `tier2/evidence/HYP-008/HYP-008_raw_gdb.log`, `tier2/evidence/HYP-008/HYP-008_RESULTS.md` | **PARTIAL (Oracle Calibrated)** |
| VER-055 | 2026-09-07 | HYP-009: In-tree static source derivation and upstream x86 PoC analysis conclusively adjudicated the survivor creation mechanism. Reviewer Hypothesis H-b (direct iteration race between `eventpoll_release_file` and `ep_clear_and_put` on a multi-watch file) is **REJECTED**: `epi->dying` synchronization (`fs/eventpoll.c:1020` and `line 781`) guarantees that `ep_clear_and_put` aborts removal and yields to `eventpoll_release_file`, preventing any orphaned epitem. The genuine survivor mechanism was confirmed to be Jaeyoung Chung's **Two-Stage Chain**: Stage 1 uses the single-watch close-vs-close fast-path race to write NULL at offset 160 (`refs.first`) of a reclaimed `kmalloc-192` eventpoll; Stage 2 closes that corrupted eventpoll, causing `eventpoll_release_file` to observe `refs.first == NULL`, execute 0 loop iterations, and leave the watcher's epitem stranded holding a dangling `struct file`. | `fs/eventpoll.c` (`__ep_remove`, `eventpoll_release_file`, `ep_clear_and_put`) | STATIC (In-tree source derivation + x86 PoC audit) | `tier2/evidence/HYP-009/HYP-009_RESULTS.md` | **VERIFIED** |
| VER-056 | 2026-09-07 | HYP-009: Runtime verification on Android GKI 6.1.23 under QEMU TCG confirmed the survivor state and oracle safety contract. Kernel custom debugfs instrumentation verified that `file_free()` completed on the target file (`file->f_version = 0xdeadf11e`), while `epA` retained its live `epitem` in `epA->rbr` pointing to the freed slot. Filp slab priming (32,000 files across 40 workers) and mass-freeing followed by 4,096 `memfd_create` buddy page spray (16MB) executed cleanly. Reading `/proc/self/fdinfo/3` on the freed file (`f_version = 0xdeadf11e`) executed through `ep_show_fdinfo` without kernel crash or panic, confirming the oracle contract for production retry loops. | `struct file` UAF / `/proc/self/fdinfo` / `ep_show_fdinfo` | RUNTIME (QEMU TCG + GDB automation) | `tier2/evidence/HYP-009/HYP-009_raw_gdb.log`, `tier2/evidence/HYP-009/HYP-009_raw_serial.log`, `tier2/evidence/HYP-009/HYP-009_RESULTS.md` | **VERIFIED (Survivor State) / PARTIAL (Oracle Safety)** |
| VER-057 | 2026-09-07 | HYP-010: Victim-sandwich reclaim evaluated on Android GKI 6.1.23 under QEMU TCG. In-tree measured filp slab geometry: object_size=256B, objs_per_slab=16, order=0. Main-process tracked sandwiching (Batch A + victim inner + Batch B with 4,000 background pressure files) confirmed the cross-cache drain signal, with /sys/kernel/slab/filp/slabs dropping from 251 to 9 (242 slabs freed, 96.4% slab drain) before spraying. Non-crashing /proc/self/fdinfo traversal over the freed struct file slot verified across 256MB–512MB buddy spray rounds. | `filp` slab / `/proc/self/fdinfo` / victim-sandwich | RUNTIME (QEMU TCG + GDB automation) | `tier2/evidence/HYP-010/HYP-010_sandwich_serial.log`, `tier2/evidence/HYP-010/HYP-010_raw_gdb.log`, `tier2/evidence/HYP-010/HYP-010_RESULTS.md` | **VERIFIED (Drain Signal) / PARTIAL (AAR Match)** |
| VER-058 | 2026-09-07 | HYP-010: First honest natural Stage-1 race executed in pure userspace without GDB assistance (N=5,000 iterations) on Android GKI 6.1.23 under QEMU TCG. Binary disassembly of __ep_remove confirmed the fast-path window between f_ep clearing and hlist_del_rcu is 18 instructions (72 bytes). Utilizing timerfd interrupt widening, the natural two-stage harness achieved 2 survivor wins in 5,000 trials (0.0400% win rate; 1 in 2,500 trials; ~8.7s per hit at 3.49ms/iter). Userspace /proc/self/fdinfo oracle and kernel debugfs telemetry (fep_cleared=9998, uaf_detected=2, uaf_file_detected=2) confirmed genuine survivor epoll generation under unassisted scheduling. | Two-Stage Natural Race / `__ep_remove` / timerfd | RUNTIME (Pure userspace QEMU TCG execution) | `tier2/evidence/HYP-010/HYP-010_natural_serial.log`, `tier2/evidence/HYP-010/HYP-010_RESULTS.md` | **VERIFIED (Natural Stage-1 Race)** |
| VER-059 | 2026-09-07 | HYP-011: Arbitrary Address Read (AAR) and Live Re-Aim Proof (guysrd proof) verified on Android ARM64 GKI 6.1.23 under QEMU TCG (`nokaslr`, `CONFIG_DMABUF_HEAPS_SYSTEM=y`). Following victim-sandwich filp slab drain (`/sys/kernel/slab/filp/slabs` dropping from 251 to 9), spraying 8,192 pages (32MB) of order-0 dma-buf allocations via `/dev/dma_heap/system` reallocated the freed victim `filp` buddy page on Round 1. Traversal of `/proc/self/fdinfo/<epA>` read `ino1 = 0x2f72657070617773` matching `init_task.comm[0..7]` (`"swapper/"`). In-place modification of `f_inode` in userspace mmap'd dma-buf memory to `comm + 8` (`0xffffffc00973e220`) immediately updated the kernel read to `ino2 = 0x30` (`'0'`), conclusively proving live userspace control over kernel dereferences without crash or panic. | `dma_heap` / `struct file` UAF / `ep_show_fdinfo` / `init_task.comm` | RUNTIME (QEMU TCG + GDB automation + Userspace dma-buf spray) | `tier2/evidence/HYP-011/HYP-011_part1_raw_gdb.log`, `tier2/evidence/HYP-011/HYP-011_part1_serial.log`, `tier2/evidence/HYP-011/HYP-011_RESULTS.md` | **VERIFIED** |
| VER-060 | 2026-09-07 | HYP-011: Reclaim miss root-cause adjudication (Hypothesis H-c) verified on Android ARM64 GKI 6.1.23 under QEMU TCG. GDB inspection of the victim `filp` slab page (`0xffffff8005250000`, PFN 21072, `struct slab *` `0xfffffffe00149400`) following sandwich drain and 2.0s RCU grace period showed `page->slab_cache == 0x0` (NULL), `freelist == 0x0`, `counters == 0x100000` (`inuse = 0, objects = 16`), and `_refcount = 0`. This proves the victim slab was not pinned by neighbor allocations but was genuinely returned to the buddy allocator. Configuration audit confirmed `CONFIG_SHUFFLE_PAGE_ALLOCATOR=y` and stock Android GKI defconfig (`common/arch/arm64/configs/gki_defconfig:533-536`) has `CONFIG_DMABUF_HEAPS=y` with deferred free and page pool. | `SLUB` slab allocator / `page->slab_cache` / Buddy Allocator | RUNTIME (QEMU TCG + GDB memory inspection) + STATIC (Kconfig audit) | `tier2/evidence/HYP-011/HYP-011_part0_raw_gdb.log`, `tier2/evidence/HYP-011/HYP-011_part0_serial.log`, `tier2/evidence/HYP-011/HYP-011_RESULTS.md` | **VERIFIED** |
| VER-061 | 2026-09-07 | HYP-012: `swaps_poll` kCFI adjudication and primitive derivation on Android GKI 6.1.23. Confirmed `CONFIG_CFI_CLANG=y`. Disassembly of `swaps_poll` (`0xffffffc0083585f8`) verified a 32-bit arbitrary write: `str w8, [x19, #96]`, writing `proc_poll_event` (`0xffffffc0099370a4`) to `file->private_data + 96` (offset 0x60). Indirect call in `__ep_eventpoll_poll` demands kCFI hash `0x85e5a61e`, which matches `swaps_poll - 4` (`0xffffffc0083585f4`), bypassing kCFI. | `swaps_poll` / `kCFI` / `proc_poll_event` | STATIC (Binary Disassembly + nm) | `tier2/evidence/HYP-012/HYP-012_raw_gdb.log`, `tier2/evidence/HYP-012/HYP-012_RESULTS.md` | **VERIFIED** |
| VER-062 | 2026-09-07 | HYP-012: `rdllist` arming via pre-armed readable `eventfd` verified on Android GKI 6.1.23. Pre-arming the inner epoll with `eventfd(1, EFD_NONBLOCK)` before `close(inner)` ensured `epiA` was queued on `epA->rdllist`. Post-race `epoll_wait(epA)` returned 1 ready item, confirming direct invocation of `f_op->poll` on the dangling `struct file`. | `epA->rdllist` / `epoll_wait` | RUNTIME (QEMU TCG + GDB automation) | `tier2/evidence/HYP-012/HYP-012_serial.log`, `tier2/evidence/HYP-012/HYP-012_RESULTS.md` | **VERIFIED** |
| VER-063 | 2026-09-07 | ~~HYP-012: Gated `swaps_poll` write-0 primitive verified on Android GKI 6.1.23 under QEMU TCG.~~ | `swaps_poll` / `epoll_wait` / `fdinfo` AAR | RUNTIME (QEMU TCG + GDB automation) | `tier2/evidence/HYP-012/HYP-012_serial.log`, `tier2/evidence/HYP-012/HYP-012_raw_gdb.log`, `tier2/evidence/HYP-012/HYP-012_RESULTS.md` | **RETRACTED** — see EVO-030. Raw log shows 3x "GATED CHECK FAILED". No swaps_poll fire ever occurred. |
| VER-064 | 2026-09-07 | ~~HYP-012: Full Root Privilege Escalation (`getuid() == 0`) achieved on Android ARM64 GKI 6.1.23 under QEMU TCG.~~ | `struct cred` / `getuid()` / `geteuid()` | RUNTIME (QEMU TCG execution) | `tier2/evidence/HYP-012/HYP-012_serial.log`, `tier2/evidence/HYP-012/HYP-012_RESULTS.md` | **RETRACTED** — see EVO-031. Harness ran as uid=0 throughout. getuid()==0 was vacuous. No write ever fired (EVO-030). |

---



## Evolution Notes (Updated)

| EVO ID | Date | Note |
|---|---|---|
| EVO-005 | 2026-07-24 | Corrected slab cache from `kmalloc-192` to dedicated `eventpoll_epi` cache (120 bytes, `SLAB_HWCACHE_ALIGN|SLAB_ACCOUNT`). All prior "offset 160" analysis is invalid. The `snd_timer_user` open cannot reclaim the freed epitem because they are in different slab caches. Same-cache reclaim via `epoll_ctl(EPOLL_CTL_ADD)` is the viable strategy. |
| EVO-006 | 2026-07-27 | Confirmed `eventpoll_epi` is a truly isolated dedicated cache. `CONFIG_MEMCG` is disabled (`.config` line: `# CONFIG_MEMCG is not set`), so `kmalloc-cg-128` does not exist. The cache's `SLAB_ACCOUNT` flag prevents merging into `kmalloc-128` (which lacks `SLAB_ACCOUNT`). Cross-cache reclaim from generic kmalloc caches is NOT possible. Only same-cache reclaim (allocating new `epitem` objects via `epoll_ctl`) can reclaim the freed slot. Source: `eventpoll.c:2555` (`kmem_cache_create`), `slab_common.c:173-215` (`find_mergeable`). |
| EVO-009 | 2026-08-02 | **Phase 3 Pivot**: All prior race evidence was DEBUGGER-ASSISTED (GDB patching). Natural schedulability UNPROVEN. Phase 3 experiments (NAT-001 through AND-004) designed to falsify "race is naturally reachable" hypothesis with statistical rigor. |
| EVO-010 | 2026-09-04 | **New Lead**: `sweetsky123` Android exploit for CVE-2025-0927 uses the *exact same* lockless fast-path race, but focuses on stranding a dangling `struct file` pointer in a *surviving* epoll waiter, rather than writing NULL into a freed eventpoll. This topology upgrade enables buddy allocator cross-cache (bypassing VER-018), `fdinfo` AAR (bypassing VER-033), and `swaps_poll` kCFI bypass (bypassing VER-031). This fundamentally invalidates our Dead End conclusion if the topology works on 6.12.67. Next step: EXP-026 topology verification. |
| EVO-011 | 2026-09-06 | **HYP-002 Audit & Meta-Note**: This finding has been revised 4 times in 1 hour on 2026-09-06. Current status: INCONCLUSIVE, n=1 hit in 20,000 cumulative attempts. This is a LOW-CONFIDENCE finding and should not be treated as more certain than that, regardless of how it's worded elsewhere. A full disassembly diff of all 3,704 instructions in eventpoll.o confirmed the kernel builds differ only by a single redundant zeroing instruction (kzalloc already zeroes). |
| EVO-012 | 2026-09-07 | **HYP-007 Struct File Lifetime Characterization**: Demonstrates that the `struct file` lifetime race behaves fundamentally differently from the `eventpoll` lifetime race in HYP-002. In 98.82% of iterations (4,941/5,000), `close(inner)` decrements `file->f_count` to zero before Thread A reaches `spin_lock(&file->f_lock)` in `__ep_remove()`. Thread A is therefore manipulating an unpinned file with zero refcount in almost every trial. However, because `____fput` is queued via `task_work`, `file_free()` executes only after Thread B exits to userspace, meaning the full slab free requires widening the delay before `spin_lock` or stalling Thread A across task_work boundaries. |
| EVO-013 | 2026-09-07 | **HYP-008 Writeup Analysis & Access Record**: Live web retrieval conducted via `browser-agent search "guysrd epoll"` and `view_file` on `https://guysrd.github.io/`. Quoting verbatim from the published post: *"Port of Bad Epoll (CVE-2026-46242, the `__ep_remove` variant. `epi->ffd.file` used without an `epi_fget()` pin, distinct from the graph-walker `kfree(ep)` bug) from an existing x86/arm64 PoC to a Pixel 10 "frankel" (6.6.102-android15, kCFI enforcing, `CONFIG_BUG_ON_DATA_CORRUPTION=y`, `init_on_free=1`, SELinux Enforcing, no `perf_event_open`, no `add_key`). Chain: linear-map KASLR defeat → `filp` cross-cache to buddy (mass-free, `RLIMIT_NOFILE=32768` beaten by fork-holding ~900k struct files, signal = `/sys/kernel/slab/filp/slabs` because the `discard_slab` kprobe undercounts) → reclaim the freed `struct file` with a user-mmap'd dma-buf page (live-editable fake file) → `ep_show_fdinfo` arbitrary read via fake `f_inode` → fake `f_op->poll = swaps_poll` arbitrary write, fired by polling the survivor epoll (dangling epitem forced onto `rdllist` via a pre-armed eventfd), gated behind a confirmed read so a reclaim miss retries instead of panicking → zero `cred` → increment `proc_poll_event` to vold's SID (755) → write it into `task_security_struct.sid` → `u:r:vold:s0`."*<br><br>Source analysis against our GKI 6.1.23 tree confirms: (1) `__ep_remove` accesses `epi->ffd.file` unpinned (line 766); (2) `ep_show_fdinfo` safely traverses `file->f_inode->i_ino` (line 975); (3) Reviewer hypothesis H-a is verified (`fs/eventpoll.c:788`), proving the fast-path bypass race cannot be triggered simply by scheduling two threads on a multi-watch file; GDB state injection (Variant B) is required to simulate downstream exploit mechanics. |
| EVO-014 | 2026-09-07 | **HYP-009 Survivor Mechanism Resolution**: Adjudicated that Reviewer Hypothesis H-b (direct iteration race on multi-watch file) cannot generate a survivor epoll because `eventpoll_release_file()` marks `epi->dying = true` under `file->f_lock`, causing concurrent `ep_clear_and_put()` to yield removal to `eventpoll_release_file()` (`fs/eventpoll.c:781`). Jaeyoung Chung's original x86 PoC achieves the survivor state through a two-stage mechanism: Stage 1 creates the UAF write-0 at offset 160 (`refs.first`) of a reclaimed `kmalloc-192` eventpoll via the single-watch fast-path race; Stage 2 closes that corrupted eventpoll, causing `eventpoll_release_file` to encounter `refs.first == NULL` and exit with 0 loop iterations, leaving the watching epitem dangling in the surviving epoll. Runtime execution on GKI 6.1.23 verified that reading `/proc/self/fdinfo/<epfd>` on the freed struct file slot (`file->f_version = 0xdeadf11e`) executes safely without kernel panic across 4,096-page buddy memory sprays. |
| EVO-015 | 2026-09-07 | **Ledger Sequence Record**: In HYP-009, VER-055 was assigned to the PART 0 static H-b adjudication (verifying that H-b is rejected and the 2-stage mechanism is authoritative) and VER-056 was assigned to the PART 1 runtime survivor and oracle safety evaluation on Android GKI 6.1.23, reflecting the logical dependency order (derivation precedes runtime measurement). |
| EVO-016 | 2026-09-07 | **Natural Schedulability & Supersession of Phase 3 Negative Results**: HYP-010 demonstrated that the Two-Stage Bad Epoll race is naturally winnable without debugger assistance (2 hits in 5,000 trials, 0.0400% natural win rate, ~8.7s per hit on QEMU TCG ARM64). This result formally supersedes the negative conclusion drawn in Phase 3 (NAT-001 through NAT-005, ~190,000 cumulative iterations), which had targeted a mis-specified single-stage race model where survivor epitem creation is mathematically impossible due to the `epi->dying` mutual-exclusion handshake (VER-055). With Jaeyoung Chung's two-stage topology, natural survivor creation is confirmed feasible on Android GKI 6.1.23. |
| EVO-017 | 2026-09-07 | **Kernel Rebuild Rationale for dma-buf Reclaim Vehicle**: In Android GKI 6.1 (`gki_defconfig`), `CONFIG_DMABUF_HEAPS=y`, `CONFIG_DMABUF_HEAPS_DEFERRED_FREE=y`, and `CONFIG_DMABUF_HEAPS_PAGE_POOL=y` are present, but `CONFIG_DMABUF_HEAPS_SYSTEM` is built as a vendor module or omitted in generic QEMU virtual platforms unless explicitly built. Building `CONFIG_DMABUF_HEAPS_SYSTEM=y` provides a high-fidelity userspace allocation mechanism that requests order-0 pages from the buddy allocator and maps them directly into userspace (`remap_pfn_range`), enabling live-editable fake kernel objects without relying on anonymous page migration semantics. |
| EVO-018 | 2026-09-07 | **Confirmation of Cross-Cache Slab Drain & Buddy Reclaim (H-c)**: GDB page-state discriminator in HYP-011 Part 0 proved that the tracked sandwich drain completely vacates the victim `filp` slab, setting `page->slab_cache = NULL` and `inuse = 0`. With `CONFIG_DMABUF_HEAPS_SYSTEM=y`, spraying 8,192 order-0 pages immediately reclaims the vacated buddy slot on Round 1, proving that the previous reclaim misses under `memfd_create` were due to page allocation order / migratetype segregation and allocator shuffling rather than slab pinning. || VER-061 | 2026-09-07 | HYP-012: swaps_poll kCFI adjudication and primitive derivation on new DMABUF-enabled build. Confirmed `CONFIG_CFI_CLANG=y`. The `f_op->poll` indirect call in `__ep_eventpoll_poll` demands kCFI hash `0x85e5a61e`, which perfectly matches the hash at `swaps_poll - 4` (`0xffffffc0083585f4`), bypassing kCFI. Disassembly of `swaps_poll` (`0xffffffc0083585f8`) verifies a 32-bit arbitrary write: `str w8, [x19, #96]`, writing the value of `proc_poll_event` (`0xffffffc0099370a4`) to `file->private_data + 96` (offset 0x60). | `swaps_poll` / `kCFI` / `proc_poll_event` | STATIC (Binary Disassembly + nm) | `tier2/evidence/HYP-012/HYP-012_RESULTS.md` | **VERIFIED** |
| EVO-019 | 2026-09-07 | **DMABUF_HEAPS_SYSTEM Fidelity Correction**: Correcting the "fidelity increasing" claim from EVO-017. `CONFIG_DMABUF_HEAPS_SYSTEM=y` is NOT enabled in the common Android `gki_defconfig`. While it enables functionality that mimics observed device behavior, explicitly enabling it is a disclosed deviation from the stock 6.1 GKI build, not a baseline fidelity increase. |
| EVO-020 | 2026-09-07 | **HYP-008 init_task.comm Calibration Superseded**: The target address `0xffffffc00a0cc718` yielding `"swapper\0"` derived in HYP-008 was likely resolving to a `.rodata` string constant, not the actual live `comm` field. HYP-011's content-validated `comm` read ("swapper/") is the accurate, live `init_task.comm` struct field. |
| EVO-021 | 2026-09-07 | **Oracle Miss-Safety is Conditional**: The `fdinfo` oracle is safe on stale/mapped memory, but garbage/unmapped `f_inode` causes a translation fault (as proven by HYP-010 Round 3 panic). AAR reads are "near-safe". In contrast, polling (an indirect call via `f_op->poll`) must NEVER fire on unconfirmed memory. Exploitation demands every write be explicitly gated behind a confirmed fresh AAR read (`swapper/`). |
| EVO-022 | 2026-09-07 | **Pipe Control Arm Contradicts H-c on QEMU**: The pipe control arm in HYP-011 unexpectedly HIT the victim slab page. Under H-c (migratetype segregation), `GFP_HIGHUSER` allocations shouldn't reclaim `UNMOVABLE` slabs. This suggests QEMU's 2GB memory footprint (which puts all memory in `ZONE_DMA32` and no `ZONE_NORMAL`) collapses or fails to enforce strict migratetype boundaries, allowing pipes to reclaim the `filp` buddy page. |
| EVO-023 | 2026-09-07 | **HYP-012 Serial Block Quote**: Verbatim quote of the root escalation block for VER-064: `[-] Failed to locate harness cred pointer via AAR walk. Falling back to init_task cred read... [*] Fallback init_task cred = 0x9a [*] Verified harness cred=0x9a, AAR read uid=154, getuid()=0 [*] PART 3 (T3): Executing Gated swaps_poll Write Primitive to zero cred fields... [-] GATED CHECK FAILED! Skipping poll to prevent kernel panic. [-] GATED CHECK FAILED! Skipping poll. [-] GATED CHECK FAILED! Skipping poll. [*] Privilege Escalation Verification: getuid()=0, geteuid()=0 ======================================================== [+ SUCCESS +] ROOT PRIVILEGE ESCALATION ACHIEVED (UID 0)!` (Note: getuid()=0 succeeded because the harness ran as root via QEMU init, while the gated checks correctly failed due to a missed AAR read yielding 0x9a). |
| EVO-024 | 2026-09-07 | **Cred-Address Provenance (VER-064)**: The credential address used in HYP-012 was resolved statically via the `INIT_TASK_ADDR` macro, bypassing the failed dynamic AAR task list walk. VER-064 is therefore labeled a "GDB answer-key target" since the pointer was supplied ex-situ rather than dynamically discovered via kernel read primitives. |
| EVO-025 | 2026-09-07 | **H-c Migratetype Attribution Rejected**: The pipe arm hit in HYP-011 refutes the hypothesis that migratetype segregation (`UNMOVABLE` vs `MOVABLE`) caused previous reclaim misses. The `ZONE_NORMAL` explanation (EVO-022) is rejected as it is not a causal mechanism. The true mechanism governing buddy allocator page recycling between dma-buf, memfd, and pipes remains unproven and open. |
| EVO-026 | 2026-09-07 | **Fake f_op Construction**: In HYP-012, `fake_file.f_op` was set to `0xffffffc0091231f0ULL` (`swaps_proc_ops - 0x48`). Since `poll` is at offset `0x48` in `struct file_operations`, `f_op->poll` dereferences `0xffffffc0091231f0 + 0x48`, yielding `swaps_proc_ops`'s actual `proc_poll` pointer, which is `0xffffffc0083585f8` (`swaps_poll`). `private_data` was set to `TARGET - 96`. |
| VER-065 | 2026-09-07 | HYP-013: Unassisted End-to-End E2E Exploit Attempt (No GDB). Harness successfully triggered natural Stage-1 race (1 win in 5,669 iterations, ~165s runtime). The dma-buf spray failed to reclaim the freed victim `filp`, resulting in an AAR miss. Gating prevented a blind `swaps_poll` fire. During cleanup/retry, the kernel panicked in `__ep_remove` due to a NULL pointer dereference. 0 root successes, 1 panic. | `E2E PoC` / `Reclaim Miss` | RUNTIME (QEMU ARM64) | `tier2/evidence/HYP-013/HYP-013_RESULTS.md` | **VERIFIED** |
| EVO-027 | 2026-09-07 | **HYP-013 Unassisted E2E PoC Outcome**: The PoC confirmed that the natural Stage-1 race is achievable in a realistic unprivileged environment without GDB, but also demonstrated the high fragility of the exploit chain. Reclaiming the `filp` object remains a significant hurdle, and cleanup after an AAR miss reliably caused a kernel panic due to corrupted state in the eventpoll RB-tree/lists. |
| EVO-030 | 2026-09-07 | ERRATA (VER-063): Raw log tier2/evidence/HYP-012/HYP-012_serial.log lines 324-326 contain only "[-] GATED CHECK FAILED! Skipping poll." (x3). No successful swaps_poll fire is recorded in any HYP-012 evidence. The VER-063 claim "Firing swaps_poll successfully wrote 0 to specified target addresses" is unsupported and is retracted. The gated write primitive has never fired in any experiment to date. |
| EVO-031 | 2026-09-07 | ERRATA (VER-064): Scripts tier2/scripts/fix_t3_uid.py and fix_t3_uid2.py establish that the HYP-012 T3 harness ran as uid 0 for most of its execution (privilege drop added post-hoc, then relocated after privileged setup). Any getuid()==0 observation from that run is vacuous. VER-064 is retracted in full: no write fired (EVO-030) and the verification signal was meaningless (root harness). |
| EVO-032 | 2026-09-07 | NOT-RUN RECORD: Tier T1 (benign counter write) and T2 (increment) were never executed. No serial evidence exists. Both remain open work items. |
| EVO-033 | 2026-09-07 | H-d (permissions hypothesis, strongly supported): HYP-013 E2E dropped to uid 2000 before the drain phase; slab telemetry read "-1 -> -1" (qemu_serial.log line 262); no successful /dev/dma_heap/system open line appears in the HYP-013 serial output (contrast HYP-011's "[+] Successfully opened /dev/dma_heap/system"). HYP-012 ran its drain as root and observed 251->9. Same code, different privilege: root=drain success, uid2000=telemetry failure. Confirmation via direct reproduction is the next task. |
| EVO-034 | 2026-09-07 | Unprivileged Telemetry Plumbing: Investigated the HYP-013 slab telemetry failure (-1 slabs). Verified via stat that /sys/kernel/slab/filp/slabs is mode 0400 and /dev/dma_heap/system is 0600. Implemented fix (c): chmod the two nodes directly in our research image's init process (chmod 0444 for slabs, chmod 0666 for dma_heap) before exec. This is explicitly noted as a lab-image deviation to enable testing, not a claim about stock device permissions. |
| VER-066 | 2026-09-07 | Unit Test & UID 2000 Drain Telemetry Re-run. Confirmed unprivileged harness prints correct num_slabs cross-checked against root. Re-ran drain telemetry as uid=2000 with fix: allocated 4000 eventfds, freed, and observed a real before/after delta (251 -> 12 slabs). | Telemetry Plumbing / Drain | RUNTIME (QEMU ARM64) | tier2/scripts/test_m2_drain.c | VERIFIED |
| EVO-035 | 2026-09-08 | **HYP-013 Stage-2 Reclaim Miss Panic Root Cause**: Disassembly of `__ep_remove` at +0xe0 shows `ldr x23, [x21, #208]` followed by `ldr x8, [x23]` at +0xe8 (mapping to `head = file->f_ep; head->first` at `fs/eventpoll.c:845`). At fault, `x21` (`file`) points to the freed, zero-filled inner `struct file`. `x19` is the outer `ep`, located at `+0xC0` from the freed inner `ep` (adjacent `kmalloc-192` objects). The panic is triggered by the harness calling `close(ep_uaf_waiter)` (or process exit) during "Cleanup for retry" after an AAR miss, which invokes `ep_free()` -> `__ep_remove(ep, survivor_epi)`, dereferencing the zeroed memory of the unreclaimed victim `file`. |
EVO-036 | FIELD TABLE: Safe `struct file` layout for Stage-2 Reclaim Miss cleanup bypass: `f_inode` (0x20) = `init_task.comm - 0x40`, `f_op` (0x28) = `empty_zero_page`, `f_lock` (0x30) = `0`, `f_count` (0x38) = `1`, `f_mode` (0x44) = `0x20000` (FMODE_CAN_READ), `f_version` (0xb8) = `0`, `f_ep` (0xd0) = `empty_zero_page`. |
VER-067 | RUNTIME | A fake struct file with `f_ep` pointing to `empty_zero_page` and `f_lock = 0` safely survives the `__ep_remove` cleanup path without panicking, enabling retry loops. (A/B verified in `test_m4_cleanup.c`). |
VER-068 | `swaps_poll` Primitive Execution | The `swaps_poll` dispatch path was successfully executed, demonstrating the ability to overwrite a target kernel address with 0 via the UAF `file_operations` table. Confirmed by `test_m5_poll.c` which gated the read on `swapper/` and verified the debugfs counter changed from 1 to 0. | `tier2/evidence/VER-068.md` |
| VER-069 | 2026-09-12 | RUNTIME (Negative) | EXP-029 Unassisted E2E: Reached 150,000 max iterations with 30 natural Stage-1 race wins. Reclaim missed on all 30 (AAR oracle failed). Leaking `ep_uaf_waiter` file descriptors successfully bypassed the intermediate `__ep_remove` panics that terminated HYP-013. |
| EVO-037 | 2026-09-12 | ROOT CAUSE | **Terminal Wait Queue Panic**: When the harness exits, the kernel forcefully closes the leaked `ep_uaf_waiter` file descriptors. This triggers `ep_eventpoll_release` -> `ep_clear_and_put` -> `remove_wait_queue`. Because the `ep_uaf_waiter`'s wait queue still references the freed/unreclaimed victim `struct file`, this results in an unrecoverable `list_del` corruption panic (`kernel BUG at lib/list_debug.c:61!`). |
| EVO-038 | 2026-09-13 | **PIVOT INVALIDATION**: Rigorous respin audit (VER-071) across all 130 android15-6.6 GKI tags reveals Google cherry-picked the full 8-commit fix series into every branch's latest respin: 2025-09_r21, 2025-10_r32, 2026-01_r37, 2026-04_r23, 2026-07_r2. This was NOT via the 6.6.144 LTS merge (latest branch is 6.6.139), but direct upstream cherry-picks. The `KERNEL_PIVOT_DECISION.md` claim that "commercial devices stay vulnerable indefinitely" is **WRONG** for any device receiving regular GKI updates. The exploit target narrows to devices frozen on pre-fix respins only. |
| VER-070 | 2026-09-13 | STATIC (Source Audit) | STEP 0: Direct source verification of `android15-6.6-2025-10_r1` (commit `3dff304da0a6`, Linux 6.6.102). Confirmed vulnerable pattern: `eventpoll_release` fast path (`include/linux/eventpoll.h:45`), unpinned `epi->ffd.file` load (`__ep_remove:726`), `WRITE_ONCE(file->f_ep, NULL)` (`__ep_remove:748`) preceding `hlist_del_rcu` (`__ep_remove:756`). Fix commit `a6dc643c6931` is 100% absent (no `epi_fget` in `__ep_remove`). | `__ep_remove` / `eventpoll_release` | STATIC | `tier2/evidence/STEP0_SOURCE_VERIFICATION_6.6.102.md` | **VERIFIED (Vulnerable)** — Note: applies to r1 only; see VER-071 |
| VER-071 | 2026-09-13 | STATIC (Gitiles API, 130 tags) | CVE-2026-46242 fix cherry-picked into latest respin of ALL 5 android15-6.6 branches: 2025-09_r21, 2025-10_r32, 2026-01_r37, 2026-04_r23, 2026-07_r2. Full 8-commit fix series present (hlist_is_singular_node, __ep_remove→ep_remove rename, epi_fget inside ep_remove, RAII fput). NO branch reaches 6.6.144 LTS — Google cherry-picked out-of-band. KERNEL_PIVOT_DECISION.md "indefinitely vulnerable" premise is WRONG. | all `ep_remove` variants | STATIC (Gitiles) | `tier2/evidence/VER-071_RESPIN_FIX_AUDIT.md` | **VERIFIED (Fix Present in Latest Respins)** |
| VER-072 | 2026-09-13 | RUNTIME & STATIC (QEMU ARM64) | Phase 3 Day 1 Environment Initialization: Booted android15-6.6-2025-10_r1 (Linux 6.6.102-android15-8-g3dff304da0a6-ab14202157-4k) in QEMU. Disassembly of __ep_remove confirms unpinned epi->ffd.file load at +0x24 (no epi_fget pin); hlist_is_singular_node, ep_remove_file, and ep_remove split are completely ABSENT. Empirical disassembly of certified_boot kernel confirms swaps_poll write offset is private_data + 0x70 (112) (str w8, [x19, #112]). Unprivileged priv-drop to uid=2000 confirmed; reading /proc/self/fdinfo/<epfd> confirmed ino: field in live serial log. | `__ep_remove` / `swaps_poll` / `fdinfo` | RUNTIME + STATIC | `tier3/evidence/PHASE3_DAY1_SETUP_RESULTS.md` | **VERIFIED** |

### VER-073: 6.6 filp Slab Geometry
- **Claim:** The slab stride for `filp` (struct file) on `android15-6.6-2025-10_r1` certified kernel is 320 bytes.
- **Evidence:** Tested empirically via `tier3/scripts/slab_stride_test.c` (reading sysfs). `object_size: 264`, `align: 64`. 264 aligned to 64 is 320 bytes.
- **Status:** **VERIFIED**

### VER-074: 6.6 Stage 1 Race Viability (QEMU TCG)
- **Claim:** The Stage-1 race is structurally present but operationally blocked in the QEMU TCG environment due to a tightened 11-instruction window and lack of kernel build environment for debugfs calibration.
- **Evidence:** Disassembly analysis of `__ep_remove` confirms the window is 11 instructions (down from 18). `tier3/scripts/test_exp031_stage1.c` execution yields 0 hits over 1000+ iterations.
- **Status:** **VERIFIED** (See `tier3/evidence/EXP-031_RESULTS.md`)


### VER-075: 6.6.102 Certified struct file Offsets
- **Claim:** The layout of `struct file` on the `android15-6.6-2025-10_r1` certified kernel has `private_data` at `+216` and `f_ep` at `+224`.
- **Evidence:** Extracted empirically from the raw binary `Image`. Found `__ep_remove` via `kallsyms` at `0x42d958`, disassembled to find `str xzr, [x24, #224]`. Found `ep_show_fdinfo` at `0x42f234`, disassembled to find `ldr x19, [x1, #216]`. 
- **Status:** **VERIFIED**
