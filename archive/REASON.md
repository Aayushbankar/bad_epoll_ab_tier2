# Archived Duplicates

These files are archived duplicates of canonical files found in `exploit/tier1/` and `docs/checkpoints/tier1_complete/`. They were copied into the `artifacts/` folder during a previous checkpointing phase. To maintain a single source of truth without deleting historical evidence, they have been relocated here.
